pipeline //Declarative pipeline always starts with pipeline keyword
{
    stages // contains all the stages which need to execute

    {
        stage ('printing the welcone message') //defining the stage
        {
            steps {echo 'Hello Jenkins'} // it tells the jenkins what action needs to perform
        }

    }
}
